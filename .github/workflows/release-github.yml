name: Release to GitHub (macOS)

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (example: v1.2.7)'
        required: false
        type: string

jobs:
  release-mac:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Resolve ref
        id: ref
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ github.event.inputs.tag }}" ]; then
            echo "ref=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
            echo "tag=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.ref.outputs.ref }}

      - name: Validate release tag
        shell: bash
        run: |
          set -euo pipefail
          case "${{ steps.ref.outputs.tag }}" in
            v*) ;;
            *)
              echo "Release tag must start with v*"
              exit 1
              ;;
          esac

      - name: Setup Node.js (LTS)
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate app icons
        run: npm run icons:generate

      - name: Fetch bundled JRE 21 (macOS arm64)
        run: npm run fetch:jre

      - name: Guard appId / bundleIdentifier
        run: npm run guard:appid

      - name: Build macOS artifacts
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
          APPLE_ID: ''
          APPLE_APP_SPECIFIC_PASSWORD: ''
          APPLE_TEAM_ID: ''
        run: npm run dist:mac

      - name: Verify updater files exist
        shell: bash
        run: |
          set -euo pipefail
          ls -lah release
          test -n "$(find release -maxdepth 1 -name '*.dmg' | head -n 1)"
          test -n "$(find release -maxdepth 1 -name '*-mac.zip' | head -n 1)"
          test -s release/latest-mac.yml

      - name: Verify mac zip structure (Squirrel.Mac)
        run: npm run verify:mac-zip

      - name: Verify code signature on app and updater zip
        env:
          REQUIRE_SPCTL: '0'
        run: npm run verify:mac-signature

      - name: Verify updater metadata consistency
        run: npm run verify:mac-consistency

      - name: Publish GitHub Release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ref.outputs.tag }}
          name: ${{ steps.ref.outputs.tag }}
          draft: false
          prerelease: false
          files: |
            release/*.dmg
            release/*-mac.zip
            release/*.blockmap
            release/latest-mac.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
